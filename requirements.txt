# AI 동향지 반자동화 - 공통 의존성 (Mac 개발 / Windows 운영)
requests>=2.31
urllib3<2            ; python_version < "3.10"   # macOS 기본 LibreSSL 대응
beautifulsoup4>=4.12
lxml>=4.9
Jinja2>=3.1
PyYAML>=6.0
Flask>=3.0
fonttools>=4.40      # 표지 글꼴을 쓰이는 글자만 잘라 담기 위해
brotli>=1.0          # woff2 압축

# 모델 제공자는 쓰는 것만 깔면 된다.
#   claude_cli  설치 불필요 (Claude Code 가 있으면 됨)
#   openai/ollama/gemini  requests 만 있으면 됨 (위에 이미 있다)
#   anthropic   아래를 깐다
# anthropic>=0.40      # Anthropic API 를 쓸 때만
