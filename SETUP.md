# 처음 쓰기 전에 — 체크리스트

## ✅ 이미 되어 있는 것

- 가상환경과 패키지
- Claude Code CLI (초안 작성용)
- 수집원 14곳 접속 확인
- 발행 기관명 설정

## 1. 텔레그램 봇 만들기 — 직접 하셔야 합니다

토큰이 곧 비밀번호라 대신 만들 수 없습니다. **5분이면 끝납니다.**

### ① 봇 만들기

텔레그램에서 **@BotFather** 검색 → 대화 시작

```
/newbot
→ 봇 이름을 물어봄       : AI 정보동향지
→ 사용자명을 물어봄      : kcg_ai_trend_bot      ← _bot 으로 끝나야 함
```

`123456789:AAH...` 형태의 토큰을 줍니다. **이 값을 복사해 두세요.**

### ② 직원들이 받아볼 방 만들기

- **채널** 권장 — 담당자만 글을 올리고 직원은 읽기만
- 방을 만든 뒤 **봇을 초대**하고, 채널이면 **관리자로 지정**해야 글이 써집니다

### ③ chat_id 알아내기

방에 아무 글이나 하나 올린 뒤, 터미널에서:

```bash
curl -s "https://api.telegram.org/bot<여기에_토큰>/getUpdates" | python3 -m json.tool | grep -B2 -A6 '"chat"'
```

`"id": -1001234567890` 을 찾습니다. **그룹·채널은 음수**입니다.

본인 계정 id 는 **@userinfobot** 에게 아무 글이나 보내면 알려 줍니다.

### ④ 설정 파일 만들기

```bash
cp config/secrets.example.yaml config/secrets.yaml
```

열어서 값을 채웁니다.

```yaml
telegram:
  token: "123456789:AAH..."
  chat_id: "-1001234567890"     # 직원 방
  admin_chat_id: "123456789"    # 본인 (초안 알림용, 없으면 직원 방으로 감)
```

### ⑤ 확인

```bash
python run.py telegram --check
```

`봇 @kcg_ai_trend_bot → 방이름 (channel)` 이 나오면 됩니다.

---

## 2. 전문 HTML 을 둘 곳 정하기 — 선택

정하면 텔레그램에 **버튼**이 붙고, 누르면 바로 열립니다.

```yaml
# config/settings.yaml
telegram:
  html_base: "https://intra.example.go.kr/ai-trend/"
```

사내 그룹웨어 게시판·포털·파일서버 어디든 **브라우저로 열리는 주소**면 됩니다.

**정하지 않아도 됩니다.** 비워 두면 HTML 파일이 첨부로 따라갑니다.
받는 쪽에서 열 때 한 단계(내려받아 브라우저로)가 더 있을 뿐입니다.

---

## 3. 첫 실행

```bash
./run_mac.command      # Mac — Finder 에서 더블클릭해도 됨
run_windows.bat        # Windows
```

메뉴에서 **3) 편집기 열기** → 브라우저가 열림 → **이번 주 동향지 만들기**

약 3~4분 걸리고 진행 상황이 단계별로 표시됩니다.

화면 위에 단계가 표시됩니다.

```
① 만들기  →  ② 손질  →  ③ 미리보기  →  ④ 발행  →  ⑤ 전송
```

- **텔레그램 버튼은 발행한 뒤에 열립니다.** 발행 전에 보내면 전문 링크가 빈 곳을 가리킵니다
- 발행 후 항목을 다시 고치면 잠기고, 다시 발행하면 열립니다
- 발행이 끝나면 "이어서 텔레그램으로 보낼까요?" 를 물어봅니다. 나중에 눌러도 됩니다

**우리청 자체 소식**(해커톤·경진대회·교육)은 수집되지 않습니다.
`+ 우리청 내부 소식 직접 추가` 로 넣으세요. 지난 11개호에서 매 호 1건씩 있었습니다.

---

## 4. 매주 자동으로 — 선택

```bash
python tools_schedule.py --day 5 --time 08:00
```

안내해 주는 한 줄을 붙여 넣으면 예약됩니다.
**초안까지만 자동**으로 만들고 담당자에게 알립니다. 발행은 확인 후 직접 누릅니다.

---

## 5. 사무실 Windows 노트북으로 옮길 때

```
1. 이 폴더를 통째로 복사 (.venv 는 빼도 됨)
2. Python 3.9 이상 설치
3. run_windows.bat 더블클릭 → 가상환경·패키지 자동 설치
4. Claude Code 설치 후 로그인
5. config/secrets.yaml 은 따로 복사 (git 에 없음)
```

## 막히면

```bash
python run.py sources          # 수집원이 다 살아 있는지
python run.py telegram --check # 봇 연결
python tools_audit.py          # 무엇을 가져와 무엇이 실렸는지
```
