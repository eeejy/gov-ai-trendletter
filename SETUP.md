# 처음 쓰기 — 30분

파이썬을 몰라도 됩니다. 순서대로 따라오시면 됩니다.

---

## 1. 프로그램 열기

**설치형을 받으셨다면** — 더블클릭하면 브라우저가 열립니다.

- 맥에서 "확인되지 않은 개발자" 라고 나오면
  → 앱을 **오른쪽 클릭 → 열기 → 열기**. 처음 한 번만 물어봅니다
- 윈도우에서 "Windows의 PC 보호" 가 나오면
  → **추가 정보 → 실행**

**소스로 쓰신다면**

```bash
python3 -m venv .venv && ./.venv/bin/pip install -r requirements.txt
python run.py editor --setup
```

---

## 2. 설정 — 여기가 전부입니다

첫 실행이면 설정 화면이 먼저 열립니다. 왼쪽에 진행 상황이 보이고, 채운 항목은
초록 점으로 바뀝니다.

### 빠른 길 — 말로 설정하기

맨 위 칸에 **한두 문장**을 적고 `제안 받기` 를 누르세요.

```
해양 안전과 선박 사고 예방을 다루는 동향지.
우리는 해양경찰청이고 현장 구조·단속 업무에 닿는 소식이 중요하다.
```

20초쯤 뒤에 키워드·수집원·트랙·관련도가 가중치와 함께 제안됩니다.

> **바로 적용되지 않습니다.** 무엇을 적용할지 체크박스로 고른 다음
> `고른 것 적용` 을 눌러야 반영됩니다. 그다음 아래에서 다듬으시면 됩니다.

**기관 이름과 "무슨 업무에 쓰는지" 를 함께 적으면** 가중치가 훨씬 잘 잡힙니다.
"국제협력 동향지" 보다 "국제협력 동향지. 우리는 OO청 국제협력담당관실이고
개도국 협력사업 기획에 쓴다" 가 낫습니다.

### ① 기관

여기 적은 이름이 **초안을 쓸 때 모델에게 그대로 전달됩니다.**
"우리 기관에는 이런 뜻" 같은 문장이 여기서 나옵니다.

### ② 수집원 — 여기서 가장 많이 막힙니다

**하나씩 `시험` 을 눌러 보세요.** 0건이면 검색어나 주소가 틀린 것입니다.

| 종류 | 언제 쓰나 |
| --- | --- |
| **정책브리핑 (부처)** | 가장 정확합니다. 부처를 고르고 검색어를 적으면 그 부처 자료만 봅니다 |
| **구글 뉴스 검색** | 어느 분야에나 됩니다. 처음 시작할 때 좋습니다 |
| **RSS 주소** | 전문지·기관 소식지. 주소가 맞는지 시험으로 꼭 확인하세요 |
| **기관 게시판** | 주소에 `selectNttList.do` 가 있는 곳이면 됩니다 |

부처 이름은 치면 목록이 뜹니다(57개).

### ③ 키워드

이 분야가 **아닌 것을 걸러내는 관문**입니다.

- **핵심어** — 제목에 하나만 있어도 통과. 12~20개쯤
- **주변어** — 여럿 겹쳐야 통과. 15~25개쯤

`이 기준으로 몇 건이 남는지 보기` 를 눌러 보세요. 최근 자료로 실제로 세어 줍니다.
**너무 좁으면 아무것도 안 남고, 너무 넓으면 엉뚱한 게 실립니다.**
통과율 40~60% 가 대체로 적당합니다.

### ④ 관련도 — 품질이 여기서 갈립니다

같은 분야 자료라도 **우리 업무에 닿는 것**이 위로 와야 합니다.

- 우리 기관을 가장 높게 (6.0쯤)
- 유사·인접 기관 (3~4)
- 먼 이야기·해외 사례 (1~2)

**"같이 있어야만 인정할 말"(context)** 이 중요합니다.
`구조·수색·단속` 처럼 **혼자서는 아무 데나 걸리는 흔한 말**에는
`해상, 선박, 어선` 을 달아 그 맥락일 때만 세게 합니다. 이걸 안 하면
전혀 다른 분야 기사가 높은 점수로 올라옵니다.

### ⑤ 구성

한 호에 몇 건을, 어떤 갈래로 나눠 실을지 정합니다.
트랙마다 최소·최대를 두면 한쪽으로 쏠리지 않습니다.

### ⑥ 모델

**안 고르셔도 수집·선별은 됩니다.** 모델은 본문 초안을 쓰는 데만 씁니다.

| | 키 | 비용 |
| --- | --- | --- |
| **Claude Code CLI** | 필요 없음 | 쓰던 구독 그대로 |
| **Gemini** | [aistudio.google.com](https://aistudio.google.com) | 무료 한도 넉넉함 |
| **Claude / GPT API** | 각 콘솔 | 종량 |
| **Ollama** (내 컴퓨터) | 필요 없음 | 무료, 대신 느림 |

키를 넣고 `시험` 을 누르면 그 자리에서 확인합니다.
키는 `config/secrets.yaml` 에 저장되고 깃에는 올라가지 않습니다.

### ⑦ 시험 — 나가기 전에 꼭 눌러 보세요

지금 설정 그대로 **실제로 수집하고 골라 봅니다.** 초안은 쓰지 않아 돈이 안 듭니다.
1~2분 걸립니다.

- **0건인 수집원**을 짚어 줍니다 → ② 로 돌아가 확인
- **하나도 못 골랐다면** → ③ 키워드를 넓히거나 ⑤ 최소 건수를 줄이세요
- 엉뚱한 게 위에 있다면 → ④ 관련도를 손보세요

---

## 3. 첫 동향지 만들기

`설정 마치고 시작` 을 누르면 편집기로 넘어갑니다.

```
① 만들기  →  ② 손질  →  ③ 미리보기  →  ④ 발행  →  ⑤ 전송
```

3~5분 걸리고 진행 상황이 단계별로 표시됩니다.

**우리 기관 자체 소식**(행사·교육·공모)은 수집되지 않습니다.
`+ 내부 소식 직접 추가` 로 넣으세요.

---

## 4. 텔레그램 — 선택

안 쓰셔도 됩니다. **폴더 열기** 로 확정본 HTML 을 꺼내 메일에 첨부해도 같습니다.

<details>
<summary>쓰신다면 (5분)</summary>

### ① 봇 만들기

텔레그램에서 **@BotFather** → `/newbot`

```
봇 이름     : 정보동향지
사용자명    : 우리기관_trend_bot     ← _bot 으로 끝나야 함
```

`123456789:AAH...` 형태의 토큰을 줍니다.

### ② 받아볼 방 만들기

**채널** 을 권합니다(담당자만 올리고 직원은 읽기만).
방을 만든 뒤 **봇을 초대**하고, 채널이면 **관리자로 지정**해야 글이 써집니다.

### ③ chat_id 알아내기

방에 아무 글이나 올린 뒤:

```bash
python run.py telegram --find
```

`"id": -1001234567890` 을 찾습니다. **그룹·채널은 음수**입니다.

### ④ 넣기

```yaml
# config/secrets.yaml
telegram:
  token: "123456789:AAH..."
  chat_id: "-1001234567890"     # 직원 방
  admin_chat_id: "123456789"    # 본인 (초안 알림용)
```

```bash
python run.py telegram --check
```

</details>

---

## 5. 매주 자동으로 — 선택

```bash
python tools_schedule.py --day 5 --time 08:00
```

**초안까지만 자동**으로 만들고 담당자에게 알립니다. 발행은 확인 후 직접 누릅니다.

---

## 막히면

```bash
python run.py doctor      # 뭐가 빠졌는지 한 번에 (여기부터)
python run.py sources     # 수집원이 다 살아 있는지
python tools_audit.py     # 무엇을 가져와 무엇이 실렸는지
```

`doctor` 는 분야 목록, 설정 파일, 고른 모델, 수집원 접속을 차례로 보고
빠진 게 있으면 **무엇을 하면 되는지** 함께 알려 줍니다.

설정을 잘못 만졌다면 **저장 직전 사본이 20개까지** 남아 있습니다
(`profiles/<분야>/.backup/`).

## 다른 컴퓨터로 옮길 때

설정은 프로그램이 아니라 **사용자 폴더**에 있습니다.

```
맥      ~/Library/Application Support/동향지
윈도우  %APPDATA%\동향지
```

이 폴더를 통째로 복사하면 그대로 옮겨집니다.
`config/secrets.yaml` 은 키라서 따로 챙기세요.
